const state = {
    roles: 0,
}
const mutations = {
    SET_ROLES(state, value) {
        state.roles = value
    }
}
const actions = {

}
export default {
    namespace: true,
    state,
    mutations,
    actions
}
